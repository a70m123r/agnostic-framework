# OS artifacts
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Working notes (private, not part of public framework)
*.private.md
.scratch/
.notes/

# Build artifacts (none yet, future-proofing)
node_modules/
dist/
build/
.cache/

# Image archives — keep originals locally, don't ship to public site
**/_archive/

# Sibling repo — agnostic-agents is its own GitHub repo (a70m123r/agnostic-agents),
# not a submodule of this one. Lives here for local convenience but pushes separately.
agnostic-agents/

# Pilot #150 ephemeral ingest artifacts (the committed data is data/raw/*.csv;
# these are the resumable-download checkpoint + log, regenerated on re-run)
pilots/1f_failsafe/data/gdelt_state.pkl
pilots/1f_failsafe/data/gdelt_ingest.log

# Python bytecode
__pycache__/
*.pyc

# Pilot 2 ground-truth raw downloads (37M; reproducible via the pipeline + locked EDI/PhysioNet URLs)
pilots/1f_failsafe_cycling/data_groundtruth/
# dev scratch from the synthetic-validation build (the committed pipeline reproduces results)
pilots/1f_failsafe_cycling/synthetic_validation/_*.py
.openrouter_key
*.key
.env
secrets.*
