# Cross-model review (2026-06-10) — latent-physics program + instinct mappings (GPT-5.5 + Gemini)

Per the standing rule, the load-bearing claims of `latent_physics_PROGRAM.md` went to GPT-5.5 + Gemini after the in-workflow Fable build + Opus skeptic pass. **Four model families have now touched this round** (Fable did the work, Opus reviewed, GPT + Gemini external). Verdicts, with the honest folds:

- **Assimilation mapping (instinct a → data assimilation / sequential Bayesian filtering): both YES.** Gemini: "sequential Bayesian filtering is the precise machinery for recursive state estimation and uncertainty reduction." GPT's condition: the mapping is real **only once ideas get explicit state + observation models** — otherwise it is metaphor. (That definition step is precisely the pilot's first job.)
- **RG/EFT mapping + semigroup caveat (instinct b/3): both YES.** Gemini: the caveat "correctly identifies the lossy, ill-posed nature of inverse reconstruction." GPT: analogy apt; "inverse recovery and multi-axis meaning-flow are overstrong/new" — i.e. the program's own flags (slices→deep-law NOT guaranteed; no off-the-shelf RG over a knowledge/meaning axis, new work) are the right flags.
- **Fragments-only / no unified field: confirmed by both.** Gemini: "no unified field exists, only disparate tools" (adds information geometry + manifold learning to the rails list). GPT: "partly — cognitive science / semantic change already cover substantial fragments" — a *fold*, not an overturn: add cognitive science to the fragments census; the no-unified-field verdict stands, consistent with the standing instrument-not-field triple-convergence.
- **Observer-relative solidity: SPLIT — the one real correction this pass.** Gemini: novel, "DA and RG assume objective reality, whereas this mirrors QBism's dependence on an observer's measurement harness." GPT: **partly covered** — by Bayesian credence, model evidence, **perspectival realism**, and **active inference** ("not DA/RG-native"). **Fold (downgrade):** the claim "no analog anywhere" is too strong. Correct form: observer-relative solidity has **no analog in the dynamics rails** (both models confirm DA assumes one global truth and RG/EFT one fixed observable algebra), but **epistemology supplies adjacent analogs** (perspectival realism, Bayesian credence, active inference, QBism). The genuinely-new move is narrower and sharper: making observer-relative solidity a **first-class parameter of a dynamics instrument bound to a render-system** — not the concept itself.
- **The shared BIGGEST_RISK (both models, effectively the same sentence):** idea-space has **no conserved Hamiltonian or metric**, so without formal operators, observables, and validation the whole physics mapping is "purely metaphorical and non-predictive" (Gemini) / "renaming existing epistemic uncertainty as physics" (GPT). This is the program's gate, stated externally: **nothing here is predictive until the pilot runs** — explicit state space + observation operator, pre-registered, `gain_v2` for welds, forecast-scored. Until then the program doc is a map of rails, not a physics.

**Net:** both instinct mappings hold as stated (with GPT's definitional condition recorded); fragments-only confirmed; solidity-novelty honestly downgraded to "new as an instrument parameter, adjacent analogs in epistemology"; the metaphor-until-operationalized risk is now the program's standing header-warning. Tier-3; convergence list stays 9; nothing promoted; pilot remains SPEC-ONLY.
