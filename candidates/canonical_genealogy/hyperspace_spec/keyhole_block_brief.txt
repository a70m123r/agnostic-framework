THE KEYHOLE BLOCK UNIVERSE - DESIGN BOUNCE

CONTEXT
We are designing (spec-only) a hyperdimensional canonical-space viewer holding a PHYSICAL and a LATENT dimension. KEYSTONE: one logarithm does four jobs - 3D-to-2D unfold v=ln(tan(pi/4+phi/2)); physical scale ladder log2(metres); render sharpness 2^(-bits); and coupling = the COIN on position (kappa = location-negentropy, sigma = 2^(-location_bits)). COIN law: rendered_sharpness(x) <= measured_bits(x); blur is the honesty badge; never render a fake measured bit. The substrate is an append-only fact-log compiled into a viewable structure; every fact wrapper carries six axes (what/when/where/who/how/why) + an observer axis (whom) + before/after links + per-axis measured_bits. OBSERVATION is an excitation-emission keyhole probe = spreading activation: shine a time-indexed concept-definition into the substrate, resonance spreads, matching wrappers ping back as sparks, capped by measured_bits.

THE EXPANSION TO PRESSURE-TEST (from Pav, the originator)
We keyhole a latent construct (e.g. democracy). The data imprints into the instrument sensor, which captures the construct SHAPE and imprints it into a MATHEMATICAL BLOCK UNIVERSE - a static N-dimensional block (spacetime + latent axes) that COMPILES incrementally. The shape is SHARPENED by MULTIPLE other keyhole streams firing in BURSTS, not just in the present but ACROSS TIME - a tomographic / multi-exposure reconstruction from many concept-angles and many time-points. As the block compiles, the structure gains FIDELITY and RESOLUTION, and MEANING derived from observation becomes KNOWLEDGE. Where the WHATs and HOWs cannot be derived they are FUZZY; and where it is fuzzy there should be a CONJECTURE - a STUB, a typed placeholder for something to latch onto (a falsifiable hole future probes can fill).

WHAT WE WANT (out-of-box, first-principles)
1. THE BLOCK UNIVERSE SUBSTRATE. Is the eternalist block universe the right ontology for the substrate - a static N-D block of facts that keyholes write into and a compiler accretes? How does observation across time work if the block is static (we increase measured_bits about a fixed past, not change it)? What is the honest relation between the append-only fact-log, the compiler, and the block? Where does a compiled block falsely look more complete or certain than its observations (the presentism / false-determinism leak)?
2. MULTI-KEYHOLE TOMOGRAPHY. Formalize many keyhole-bursts (across concept-angles AND time) as projections that reconstruct a latent construct shape (computed tomography / Radon transform / synthetic aperture / multi-view reconstruction / compressed sensing). What is the FIDELITY LAW - how do fidelity and resolution grow with the number, diversity, and INDEPENDENCE of bursts, bounded by the COIN? When does the reconstruction lock? How do limited-angle gaps become honest fuzz / stubs rather than streaking artifacts (fake bits)?
3. OBSERVATION -> MEANING -> KNOWLEDGE. Where is the line between observation, meaning, and knowledge, in measured-bits terms? When does corroborated observation become knowledge (a disclosed threshold / the Kolmogorov structure-function kink)? Is meaning derived from observation a discovery or a construction (the participatory back-reaction)?
4. CONJECTURE-STUBS. Design the conjecture-stub: a typed placeholder at a fuzzy axis where WHAT/HOW/WHY cannot be derived; a latch-point a future probe can fill; falsifiable; with a lifecycle (conjecture -> experiment -> corroborate / demote). How does a stub render so it never silently hardens into a fact? How does the instrument use fuzzy regions to AIM the next keyhole-burst (active learning / optimal experimental design)?
5. THE BIGGEST RISK and the ONE experiment or illustration that settles whether the block-universe-compiled-by-keyholes model is real machinery or a pretty metaphor.

FORMAT
Concrete. Math or mechanism, not vibes. Name prior art with the actual technique (block universe / eternalism, Radon transform / CT, compressed sensing, multi-view 3D / gaussian splatting, active learning / optimal experimental design, truth-maintenance / abduction, spreading activation). Flag where you speculate. Disagreeing is welcome. ASCII only.
